To run the project.

1. Download the code from the master branch
2. Extract all files and folders within "currency-app-master"
3. Open the node.js command line and navigate to folder containing the extracted files and folders
4. Run the "npm install" command
5. Run the "npm start" command
